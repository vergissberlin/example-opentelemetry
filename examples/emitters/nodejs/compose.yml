name: examples-opentelemetry-emitter-nodejs

services:
  nodejs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: examples-opentelemetry-emitter-nodejs
    ports:
      - "8030:8030"

networks:
  default:
    external: true
    name: examples-opentelemetry-default
